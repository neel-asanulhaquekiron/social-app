# Copy to .env and fill in. These are public (bundled into the app) — never put secrets here.
EXPO_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=<supabase publishable/anon key>
EXPO_PUBLIC_API_BASE_URL=https://<your-api-host>
